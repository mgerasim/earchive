<table>
  <tr>
    <th>Логин</th>
    <th>Филиал</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @workers.each do |worker| %>
  <tr>
    <td><%= worker.nickname %></td>
    <td><%= worker.depart.name %></td>
    <td><%= link_to 'Показать', worker %></td>
    <td><%= link_to 'Редактировать', edit_worker_path(worker) %></td>
    <td><%= link_to 'Удалить', worker, confirm: 'Вы уверены?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новый работник', new_worker_path %>
